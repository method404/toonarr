services:
  toonarr:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - toonarr-data:/app/data
    restart: unless-stopped

volumes:
  toonarr-data:
